<h1>Where you can learn to code</h1>
<p>Below are a number of online sites that have coding tests and would help you better learn to code.</p>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Image</th>
      <th>Site</th>
      <th>Description</th>
      <th>Free</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @learns.each do |learn| %>
      <tr>
        <td><h3><%= learn.title %></h3></td>
        <td><%= learn.image %></td>
        <td><%=link_to learn.site %></td>
        <td><%= learn.description %></td>
        <td><%= learn.free %></td>
        <td><%= link_to 'Show', learn %></td>
        <td><%= link_to 'Edit', edit_learn_path(learn) %></td>
        <td><%= link_to 'Destroy', learn, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add a new learning resource', new_learn_path, :class => 'button' %>
